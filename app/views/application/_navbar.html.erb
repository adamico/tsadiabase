<nav class="navbar navbar-main navbar-fixed-top" role="navigation">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <% 3.times do -%>
          <span class="icon-bar"></span>
        <% end -%>
      </a>
      <a class="brand" href="/">Tsadiabase</a>
      <div class="nav-collapse">
        <ul class="nav">
          <%= content_tag :li, link_to("Accueil", "/"), class: request.path == "/" ? "active" : nil %>
          <%= content_tag :li, link_to("Saisie", new_patient_path), class: request.path == new_patient_path ? "active" : nil %>
          <%= content_tag :li, link_to("Liste", patients_path), class: request.path == patients_path ? "active" : nil %>
        </ul>
      </div><!--nav-collapse-->
    </div><!--container-->
  </div><!--navbar-inner-->
</nav>
